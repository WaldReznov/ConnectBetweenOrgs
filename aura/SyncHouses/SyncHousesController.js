({
    syncHousesFront : function(component, event, helper) {
        helper.syncHousesHelper(component, event, helper);
    },

    deleteHousesFront : function(component, event, helper) {
        helper.deleteHousesHelper(component, event, helper);
    }
})